# Railway configuration for Backend
[build]
builder = "nixpacks"

[deploy]
startCommand = "alembic upgrade head && uvicorn app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

