# Railway configuration for Backend
[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'alembic upgrade head && uvicorn app:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/"
healthcheckTimeout = 180
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5


