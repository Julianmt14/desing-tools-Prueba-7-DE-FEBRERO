# Railway configuration for Backend
[build]
builder = "nixpacks"

[deploy]
startCommand = "alembic upgrade head && uvicorn app:app --host 0.0.0.0 --port ${PORT:-8001}"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
