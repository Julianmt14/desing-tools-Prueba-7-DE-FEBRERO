# Railway configuration for Frontend
[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npx serve build --listen tcp://0.0.0.0:$PORT"
healthcheckPath = "/"
healthcheckTimeout = 60


